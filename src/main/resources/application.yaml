spring:
  application:
    name: hms 
  datasource:
    url: jdbc:${DB_URL:mysql://localhost:3306/hms_db?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Sahil$123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: ${PORT:8085}
admin:
  email: ${ADMIN_EMAIL:admin@hms.com}
  password: ${Admin_password:admin123};